{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "调试 ml_app (CMake)",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/Debug/ml_app.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "console": "integratedTerminal",
            "preLaunchTask": "CMake: build (Debug)"
        }
    ]
}
